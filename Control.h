#ifndef CONTROL_H_INCLUDED
#define CONTROL_H_INCLUDED

#include <SFML/Graphics.hpp>
#include <SFML/Window.hpp>
#include <iostream>

/*
string keyboardControl()
{
    if (sf::Keyboard::isKeyPressed(sf::Keyboard::Left))
            {
                float X=herosprite.getPosition().x;
                float Y=herosprite.getPosition().y;
                std::cout<<"X: "<<X<<"\n";
                std::cout<<"Y: "<<Y<<"\n";
                if(X>=1 && X<=599)
                {
                    herosprite.setPosition(X-1,Y);
                }
            }

    if (sf::Keyboard::isKeyPressed(sf::Keyboard::Right))
            {
                float X=herosprite.getPosition().x;
                float Y=herosprite.getPosition().y;
                std::cout<<"X: "<<X<<"\n";
                std::cout<<"Y: "<<Y<<"\n";
                if(X>=1 && X<=599)
                {
                    herosprite.setPosition(X+1,Y);
                }
            }

    if (sf::Keyboard::isKeyPressed(sf::Keyboard::Up))
            {
                float X=herosprite.getPosition().x;
                float Y=herosprite.getPosition().y;
                std::cout<<"X: "<<X<<"\n";
                std::cout<<"Y: "<<Y<<"\n";
                if(Y>=1 && Y<=599)
                {
                    herosprite.setPosition(X,Y-1);
                }
            }

    if (sf::Keyboard::isKeyPressed(sf::Keyboard::Down))
            {
                float X=herosprite.getPosition().x;
                float Y=herosprite.getPosition().y;
                std::cout<<"X: "<<X<<"\n";
                std::cout<<"Y: "<<Y<<"\n";
                if(Y>=1 && Y<=599)
                {
                    herosprite.setPosition(X,Y+1);
                }
            }


    if (sf::Keyboard::isKeyPressed(sf::Keyboard::Space))
            {
                float X=herosprite.getPosition().x;
                float Y=herosprite.getPosition().y;
                std::cout<<"X: "<<X<<"\n";
                std::cout<<"Y: "<<Y<<"\n";
                herosprite.setPosition(300,300);

            }

}
*/

#endif // CONTROL_H_INCLUDED

/*



if (sf::Keyboard::isKeyPressed(sf::Keyboard::Left))
            {
                float X=herosprite.getPosition().x;
                float Y=herosprite.getPosition().y;
                std::cout<<"X: "<<X<<"\n";
                std::cout<<"Y: "<<Y<<"\n";
                if(X>=1 && X<=599)
                {
                    herosprite.setPosition(X-1,Y);
                }
            }

    if (sf::Keyboard::isKeyPressed(sf::Keyboard::Right))
            {
                float X=herosprite.getPosition().x;
                float Y=herosprite.getPosition().y;
                std::cout<<"X: "<<X<<"\n";
                std::cout<<"Y: "<<Y<<"\n";
                if(X>=1 && X<=599)
                {
                    herosprite.setPosition(X+1,Y);
                }
            }

    if (sf::Keyboard::isKeyPressed(sf::Keyboard::Up))
            {
                float X=herosprite.getPosition().x;
                float Y=herosprite.getPosition().y;
                std::cout<<"X: "<<X<<"\n";
                std::cout<<"Y: "<<Y<<"\n";
                if(Y>=1 && Y<=599)
                {
                    herosprite.setPosition(X,Y-1);
                }
            }

    if (sf::Keyboard::isKeyPressed(sf::Keyboard::Down))
            {
                float X=herosprite.getPosition().x;
                float Y=herosprite.getPosition().y;
                std::cout<<"X: "<<X<<"\n";
                std::cout<<"Y: "<<Y<<"\n";
                if(Y>=1 && Y<=599)
                {
                    herosprite.setPosition(X,Y+1);
                }
            }


    if (sf::Keyboard::isKeyPressed(sf::Keyboard::Space))
            {
                float X=herosprite.getPosition().x;
                float Y=herosprite.getPosition().y;
                std::cout<<"X: "<<X<<"\n";
                std::cout<<"Y: "<<Y<<"\n";
                herosprite.setPosition(300,300);

            }


*/
